# Common

```bash
npm run generate:resource:all-db -- --name Category
--------
npm run migration:generate -- src/database/migrations/xxx
npm run migration:run
--------
npm run seed:create:relational -- --name Category
--------
加属性
npm run add:property:to-all-db
```
